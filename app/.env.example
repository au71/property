# Browser-visible API origin. Must be reachable from the user's device.
NEXT_PUBLIC_API_URL=http://localhost:4000

# Server-side API origin used by server components and route handlers.
# Usually the same, but can be an internal address in a container network.
API_URL=http://localhost:4000
